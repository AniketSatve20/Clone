{
  "name": "humanwork-ai-worker",
  "version": "1.0.0",
  "description": "AI-powered dispute resolution worker for HumanWork Protocol",
  "main": "dist/listener.js",
  "scripts": {
    "start": "ts-node src/listener.ts",
    "build": "tsc",
    "dev": "ts-node src/listener.ts",
    "db:init": "ts-node -e \"import { initializeDatabase } from './src/database'; initializeDatabase();\""
  },
  "dependencies": {
    "better-sqlite3": "^9.6.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.8.0",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.7",
    "@types/express": "^4.17.25",
    "@types/node": "^20.8.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.0"
  }
}
